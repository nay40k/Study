package aircompany.aircraft;

public class Airplane extends Aircraft {
    public Airplane(String model, int capacity, double cargoCapacity, int flightRange) {
        super(model, capacity, cargoCapacity, flightRange);
    }
}